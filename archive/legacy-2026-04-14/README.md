# Legacy Archive - 2026-04-14

These files were moved out of the active app path during the portfolio cleanup.

## Contents
- `out/` - generated static export output. Recreate with `npm run build`.
- `infrastructure/` - older deployment and monitoring files that are not used by the current local Next app flow.
- `public-starter-assets/` - unused Next starter SVGs.
- `config/tailwind.config.js` - unused Tailwind config. Tailwind is not installed or used by the current app.
- `tests/performance-baselines.yml` - old performance baseline file with no active test runner in `package.json`.

## Active Assets Kept
- `public/profile.jpg` remains active and is used by the portfolio hero.
- `src/app/favicon.ico` remains active and is used by Next.
